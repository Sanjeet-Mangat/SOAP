<UserControl x:Class="client.ParameterEntry"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="auto" />
        </Grid.ColumnDefinitions> 
        
        <TextBlock x:Name="parameterName" Text="name" VerticalAlignment="Center" FontSize="20" />
        <TextBox x:Name="parameterValue" VerticalAlignment="Center" FontSize="20" Grid.Column="1" />
        <TextBlock x:Name="parameterType" Text="type" VerticalAlignment="Center" FontSize="20" Grid.Column="2" />
    </Grid>
</UserControl>
